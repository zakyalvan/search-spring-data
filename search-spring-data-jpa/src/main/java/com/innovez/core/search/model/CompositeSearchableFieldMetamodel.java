package com.innovez.core.search.model;

/**
 * Contract form composite search-able field meta-model. This meta-model used for representing
 * search-able fields, in case the field is search-able type also (Contain searchable type properties).
 * 
 * @author zakyalvan
 */
public interface CompositeSearchableFieldMetamodel extends SearchableMetamodel, SearchableFieldMetamodel {
	
}
